  /* Haz uso de nomenclaturas correctas en la variables
     mantener los nombres dados
  */
     (() => {
      class WordToWord {} // Clase con nomenclatura PascalCase
     
      interface Props {} // Interfaz con nomenclatura PascalCase
     
      type Moves = {}; // Tipo con nomenclatura PascalCase
     
      function getFood() {} // Función con nomenclatura camelCase
     
      function setProductName() {} // Función con nomenclatura camelCase
     
      type Directions = {}; // Tipo con nomenclatura PascalCase
     
      interface IDBProps {} // Interfaz con nomenclatura PascalCase
     
      class AToBToC {} // Clase con nomenclatura PascalCase
     
      // Las constantes se definen con const y nomenclatura camelCase
      const constantOne = 3;
      const constantTwo = 4;
     })();